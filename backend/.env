# Environment Variables for Plote. Backend

# Port for the Express server to listen on
PORT=5000

# PostgreSQL Database Connection URL (from Render, Supabase, ElephantSQL, etc.)
# Example: postgresql://user:password@host:port/database_name?sslmode=require
# MAKE SURE to replace user, password, host, port, and database_name with your actual credentials.
# The `?sslmode=require` or `?sslmode=no-verify` part might be necessary depending on your provider's SSL setup.
# For Render's PostgreSQL, it often looks like: postgresql://user:password@hostname/database_name
DATABASE_URL=postgresql://neondb_owner:npg_WMpkymcjX47r@ep-empty-cell-a8593amd-pooler.eastus2.azure.neon.tech/neondb?sslmode=require

# Secret key for JSON Web Tokens (JWT)
# Generate a long, random, and complex string for this!
JWT_SECRET="supersecretjwtkeythatisverylongandrandomandsecure12345!@#$"

# URL of your deployed frontend (for CORS and password reset links if implemented in future)
# Example: http://localhost:5173 (for local dev) or https://plote-frontend.onrender.com
FRONTEND_URL=https://kroxnest.onrender.com

# Email credentials for sending password reset emails (if you implement email sending)
# This feature is not implemented in the provided code, but placeholders are here.
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password_or_app_password
